#ifndef CODES_H
#define CODES_H

const char* wifi_ssids[] = {"ssid1", "ssid2", "ssid3"};
const char* wifi_passwords[] = {"pwd1", "pwd2", "pwd3"};
const int wifi_network_count = sizeof(wifi_ssids) / sizeof(wifi_ssids[0]);

const char* scriptURL = "https://script.google.com/macros/s/...";

#endif